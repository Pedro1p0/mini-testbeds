# mini-testbeds
Um repositório no GitHub que provisiona automaticamente um cluster Kubernetes local usando kind, configura o ambiente com Ansible, instala uma aplicação via Helm e tudo isso disparado por uma pipeline GitHub Actions.
